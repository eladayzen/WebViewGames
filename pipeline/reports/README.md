# reports

Stage 1 output — one `.md` report per topic folder in `../videos-inbox/`
(covering all videos + notes.txt in that folder together), written by the
`video-analyst` subagent. Generated, not hand-authored. See
`../../PIPELINE.md`.
