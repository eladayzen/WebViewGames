# macro-briefs/approved

Brief folders (`brief.md` + `concepts/`) that have been explicitly
human-approved (via `/approve-brief`, which moves them here from
`../proposed/`). Only briefs here are eligible for stage 3
(`/write-brief`) — never expand one still sitting in `../proposed/`. See
`../../../PIPELINE.md`.
