# macro-briefs/approved

Macro briefs that have been explicitly human-approved (via
`/approve-brief`). Only briefs here are eligible for stage 3
(`/write-brief`) — never expand one still sitting in `../proposed/`. See
`../../../PIPELINE.md`.
