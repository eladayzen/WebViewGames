# macro-briefs/proposed

Stage 2 output — candidate game concepts, written by the
`macro-game-designer` subagent (via `/propose-briefs`). Awaiting human
review. Approve one with `/approve-brief <slug>`, which moves it to
`../approved/`. See `../../../PIPELINE.md`.
