# build-docs

Stage 3 output — complete, execution-ready build docs, written by the
`brief-writer` subagent (via `/write-brief`) from an approved macro brief.
Same depth/format as `../../TmntRunner/gdd-ninja-runner.md`. Feed one of
these to `/build-game` to run stage 4. See `../../PIPELINE.md`.
